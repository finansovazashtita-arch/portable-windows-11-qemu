FinansProtect Enterprise has been successfully deployed.

1. Get the application URLs:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else }}
  export NODE_PORT=$(kubectl get --namespace {{ .Values.global.namespace | default "finansprotect" }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "finansprotect.fullname" . }}-dashboard)
  export NODE_IP=$(kubectl get nodes --namespace {{ .Values.global.namespace | default "finansprotect" }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- end }}

2. Check the status of your deployments:
  kubectl get pods -n {{ .Values.global.namespace | default "finansprotect" }} -l "app.kubernetes.io/name={{ include "finansprotect.name" . }}"

3. Note about Infisical integration:
{{- if .Values.infisical.enabled }}
  Ensure the Infisical operator is running and the credentials are valid for vault connection.
{{- else }}
  Infisical integration is currently disabled.
{{- end }}
